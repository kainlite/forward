# permissions for end users to view mapports.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: forward2
    app.kubernetes.io/managed-by: kustomize
  name: mapport-viewer-role
rules:
- apiGroups:
  - forward.redbeard.team
  resources:
  - mapports
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - forward.redbeard.team
  resources:
  - mapports/status
  verbs:
  - get
